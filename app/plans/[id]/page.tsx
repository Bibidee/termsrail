export default async function PlanDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;return <div className="shell"><p className="kicker">PLAN / {id}</p><h1>Agent Plan {id}</h1><p className="lede">Canonical plan details and authorization state.</p><section className="panel"><h2>CONNECT WALLET TO LOAD CANONICAL PLAN</h2><p>Plan hash, step verdicts and execution gate will appear from the deployed registry.</p></section></div>}
