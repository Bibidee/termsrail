'use client';
import Link from 'next/link';import {useState} from 'react';import {clientFor,connectWallet,writeAndRead,requireContract,Eip1193,type LifecycleEvent} from '../../../lib/genlayer';
export default function NewPlan(){const[title,setTitle]=useState(''),[description,setDescription]=useState(''),[serviceId,setServiceId]=useState('0'),[actionId,setActionId]=useState('0'),[status,setStatus]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();try{const p=(window as Window&{ethereum?:Eip1193}).ethereum;if(!p)throw Error('Wallet unavailable');const a=await connectWallet(p),c=clientFor(a as `0x${string}`,p),steps=JSON.stringify([{service_id:serviceId,action_id:actionId,required:true}]);setStatus('SUBMITTING');await writeAndRead(a as `0x${string}`,p,'create_plan',[title,description,steps],()=>c.readContract({address:requireContract(),functionName:'get_plans',args:[0n,50n] as never[]}) as Promise<string[]>,rows=>rows.some(x=>{try{return JSON.parse(x).title===title}catch{return false}}),(x:LifecycleEvent)=>setStatus(x.phase));setStatus('PLAN CREATED · CANONICAL READBACK COMPLETE')}catch(e){setStatus(e instanceof Error?e.message:'Plan creation failed')}};return <div className="shell narrow"><p className="kicker">PLAN BUILDER / 01</p><h1>Create an Agent Plan</h1><p className="lede">Bind a bounded sequence to canonical services and actions.</p><form className="form" onSubmit={submit}><fieldset><legend>01 — PLAN</legend><label>Title<input required value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Description<textarea required value={description} onChange={e=>setDescription(e.target.value)}/></label></fieldset><fieldset><legend>02 — FIRST STEP</legend><label>Service ID<input required value={serviceId} onChange={e=>setServiceId(e.target.value)}/></label><label>Action ID<input required value={actionId} onChange={e=>setActionId(e.target.value)}/></label></fieldset><button className="button primary">CREATE PLAN →</button><Link className="button" href="/plans">CANCEL</Link>{status&&<div className="tx-progress">{status}</div>}</form></div>}
