import Link from 'next/link';
const dimensions=[['AUTOMATION','CONDITIONAL'],['SCRAPING','PROHIBITED'],['COMMERCIAL USE','ALLOWED'],['REDISTRIBUTION','PROHIBITED'],['MODEL TRAINING','RESTRICTED'],['ACCOUNT AUTOMATION','CONDITIONAL']];
export default function Home(){return <div className="shell"><section className="hero"><div><p className="kicker">POLICY EXECUTION BOARD / LIVE CHAIN STATE</p><h1>Decide before<br/><em>the agent acts.</em></h1><p className="lede">TermsRail turns published service policy into versioned, consensus-backed execution signals. Every verdict is bound to its source universe, policy version, action specification and freshness window.</p><div className="actions"><Link className="button primary" href="/services/new">REGISTER SERVICE <span>→</span></Link><Link className="button" href="/action/new">REGISTER ACTION</Link></div></div><div className="signal-panel"><div className="signal-head"><span className="lamp on"></span><span className="lamp"></span><span className="lamp"></span></div><div className="signal-word">NO ACTIVE GATE</div><small>CONNECT WALLET TO READ YOUR BOARD</small></div></section><section className="board-grid"><div className="panel"><div className="panel-title"><span>POLICY RAIL</span><span className="mono">SERVICE / —</span></div><div className="rail"><span>POLICY V000</span><i></i><b>NEEDS SNAPSHOT</b></div><p className="muted">Your registered services and accepted policy snapshots appear here. Empty chain state is shown truthfully.</p><Link href="/services/new" className="text-link">+ Add first service</Link></div><div className="panel"><div className="panel-title"><span>CONTROLLED DIMENSIONS</span><span className="mono">EXAMPLE SCHEMA</span></div><div className="dimension-list">{dimensions.map(([n,v])=><div className="dimension" key={n}><span>{n}</span><strong className={v==='PROHIBITED'?'stop':v==='RESTRICTED'?'caution':'proceed'}>{v}</strong></div>)}</div><p className="muted">Bounded observations only. Raw policy prose never becomes authoritative state.</p></div></section><section className="three-path"><div><span className="step">01</span><h3>SNAPSHOT CONSENSUS</h3><p>Validators independently fetch configured sources and reduce operative meaning into policy dimensions.</p></div><div><span className="step">02</span><h3>ACTION AUTHORIZATION</h3><p>Exact structured actions are compared against the accepted snapshot. Deterministic code derives the verdict.</p></div><div><span className="step">03</span><h3>CHANGE DETECTION</h3><p>Re-checks distinguish material policy meaning changes from navigation and formatting noise.</p></div></section></div>}
