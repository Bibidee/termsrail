import Link from 'next/link';
const states=[['ALLOWED','✓','proceed'],['CONDITIONAL','!','caution'],['RESTRICTED','!','caution'],['PROHIBITED','×','stop'],['UNKNOWN','?','unknown'],['CONFLICTING','≠','conflict']];
export default function Home(){return <div className="shell"><section className="hero"><div><p className="kicker">AGENT POLICY INFRASTRUCTURE</p><h1>Agents move fast.<br/><em>Terms change faster.</em></h1><p className="lede">TermsRail turns live service policies into consensus-backed execution gates, so autonomous agents can check the rules before they act.</p><div className="actions"><Link className="button primary" href="/services/new">REGISTER A SERVICE <span>→</span></Link><Link className="button" href="/about">SEE HOW IT WORKS</Link></div></div><div className="signal-panel"><div className="panel-title"><span>POLICY ROUTE</span><span className="mono">LIVE CHECKPOINTS</span></div><div className="route-stack"><div className="route-node blue">SOURCE <b>01</b></div><div className="route-line"/><div className="route-node green">SNAPSHOT <b>V2</b></div><div className="route-line"/><div className="route-node orange">ACTION <b>SPEC</b></div><div className="route-line short"/><div className="route-verdict">● GO</div></div><div className="route-branches"><span>ALLOW</span><span>CHECK</span><span>STOP</span></div></div></section><section className="board-grid"><div className="panel"><div className="panel-title"><span>LIVE POLICY BOARD</span><span className="mono">CANONICAL STATE</span></div><div className="empty-state"><h2>The tracks are quiet.</h2><p>Register your first service and TermsRail will start tracking its policy state.</p><Link href="/services/new" className="button primary">REGISTER SERVICE →</Link></div></div><div className="panel"><div className="panel-title"><span>POLICY CHECKS</span><span className="mono">STATE VOCABULARY</span></div><div className="dimension-list">{states.map(([name,symbol,tone])=><div className="dimension" key={name}><span>{name}</span><strong className={tone}>{symbol} {name==='ALLOWED'?'GO':name==='PROHIBITED'?'STOP':name==='CONDITIONAL'||name==='RESTRICTED'?'CHECK':'REVIEW'}</strong></div>)}</div><p className="muted">Every verdict keeps its canonical state and a plain-language signal. Colour never carries meaning alone.</p></div></section><section className="three-path"><div><span className="step">01</span><h3>SNAPSHOT CONSENSUS</h3><p>Validators compare configured sources and reduce operative meaning into policy dimensions.</p></div><div><span className="step">02</span><h3>ACTION AUTHORIZATION</h3><p>Exact structured actions are checked against the accepted snapshot before a gate opens.</p></div><div><span className="step">03</span><h3>CHANGE DETECTION</h3><p>Re-checks distinguish material policy meaning changes from navigation and formatting noise.</p></div></section></div>}
