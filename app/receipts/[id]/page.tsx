export default async function ReceiptDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;return <div className="shell"><p className="kicker">RECEIPT / {id}</p><h1>Authorization Receipt {id}</h1><p className="lede">Inspect the plan hash, policy versions and validity status.</p><section className="panel"><h2>CONNECT WALLET TO LOAD CANONICAL RECEIPT</h2></section></div>}
