'use client';
import {useState} from 'react';
export default function NewAction(){const [status,setStatus]=useState('');return <div className="shell narrow"><p className="kicker">ACTION REGISTRY / STRUCTURED SPECIFICATION</p><h1>Register an action</h1><p className="lede">Bind an exact proposed action to a service. The specification hash travels with every authorization.</p><form className="form" onSubmit={e=>{e.preventDefault();setStatus('ACTION REGISTERED · SPEC HASH PENDING READBACK')}}><label>Service ID<input required placeholder="service id"/></label><label>Action key<input required placeholder="catalog-pages-30m"/></label><label>Action type<select defaultValue="DATA_COLLECTION"><option>DATA_COLLECTION</option><option>API_CALL</option><option>ACCOUNT_ACTION</option><option>MODEL_TRAINING</option><option>DATA_REDISTRIBUTION</option></select></label><label>Description<textarea required placeholder="Collect public product pages every 30 minutes, store data, redistribute commercially."/></label><div className="form-row"><label>Frequency<input placeholder="every 30 minutes"/></label><label>Volume class<select><option>LOW</option><option>MEDIUM</option><option>HIGH</option></select></label></div><button className="button primary">REGISTER ACTION →</button>{status&&<div className="tx-progress">{status}</div>}</form></div>}
