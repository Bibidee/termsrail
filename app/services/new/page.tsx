'use client';
import {useState} from 'react';
export default function NewService(){const [status,setStatus]=useState('');const [name,setName]=useState(''); return <div className="shell narrow"><p className="kicker">SERVICE REGISTRY / NEW ENTRY</p><h1>Register a service</h1><p className="lede">Define the complete source universe. Updating it increments source_version and closes all existing execution gates.</p><form className="form" onSubmit={e=>{e.preventDefault();setStatus('SUBMITTING → LEADER EXECUTION → VALIDATOR REVIEW → CONSENSUS → FINALISED → CANONICAL READBACK')}}><label>Service key<input required value={name} onChange={e=>setName(e.target.value)} placeholder="acme-commerce"/></label><label>Display name<input required placeholder="Acme Commerce"/></label><label>Domain<input required placeholder="acme.example"/></label><label>Policy sources<textarea required defaultValue="https://example.com/terms-of-service\nhttps://example.com/acceptable-use"/></label><p className="hint">One HTTPS URL per line. Credentials, localhost, private IPs and duplicates are rejected on-chain.</p><button className="button primary">REGISTER SERVICE →</button>{status&&<div className="tx-progress">{status}<br/><small>Authoritative readback required before success.</small></div>}</form></div>}
