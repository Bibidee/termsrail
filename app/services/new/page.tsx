'use client';
import {useState} from 'react'; import {clientFor,connectWallet,CONTRACT_ADDRESS,Eip1193} from '../../../lib/genlayer'; import {useRouter} from 'next/navigation';
export default function NewService(){const [form,setForm]=useState({key:'',name:'',domain:'',url:'',role:'TERMS_OF_SERVICE'});const [status,setStatus]=useState('');const router=useRouter(); const submit=async(e:React.FormEvent)=>{e.preventDefault();try{const p=(window as Window&{ethereum?:Eip1193}).ethereum;if(!p)throw Error('Install an injected wallet first');const a=await connectWallet(p);const c=clientFor(a as `0x${string}`,p);setStatus('SUBMITTING…');const hash=await c.writeContract({address:CONTRACT_ADDRESS as `0x${string}`,functionName:'register_service',args:[form.key,form.name,form.domain,form.url,form.role,86400] as never[],value:0n});setStatus(`FINALIZING ${hash}…`);await c.waitForTransactionReceipt({hash,status:'FINALIZED' as never});const rows=await c.readContract({address:CONTRACT_ADDRESS as `0x${string}`,functionName:'get_services',args:[0n,50n] as never[]}) as string[];const id=rows.length-1;setStatus(`FINALIZED · SERVICE ${id} READ BACK`);router.push(`/service/${id}`)}catch(err){setStatus(err instanceof Error?err.message:'Transaction failed')}}; return <div className="shell narrow"><p className="kicker">SERVICE REGISTRY / NEW ENTRY</p><h1>Register a service</h1><p className="lede">Define the source universe used by policy consensus.</p><form className="form" onSubmit={submit}><label>Service key<input required value={form.key} onChange={e=>setForm({...form,key:e.target.value})}/></label><label>Display name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Domain<input required value={form.domain} onChange={e=>setForm({...form,domain:e.target.value})}/></label><label>Policy source URL<input required type="url" value={form.url} onChange={e=>setForm({...form,url:e.target.value})}/></label><label>Source role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>TERMS_OF_SERVICE</option><option>API_TERMS</option><option>SCRAPING_POLICY</option><option>DATA_POLICY</option></select></label><button className="button primary">REGISTER SERVICE →</button>{status&&<div className="tx-progress">{status}</div>}</form></div>}
