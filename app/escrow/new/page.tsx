'use client';
import Link from 'next/link';import {useState} from 'react';import {clientFor,connectWallet,writeAndRead,requireContract,Eip1193,type LifecycleEvent} from '../../../lib/genlayer';
export default function NewEscrow(){const[planId,setPlanId]=useState('0'),[recipient,setRecipient]=useState(''),[amount,setAmount]=useState('1'),[deadline,setDeadline]=useState(''),[status,setStatus]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();try{const p=(window as Window&{ethereum?:Eip1193}).ethereum;if(!p)throw Error('Wallet unavailable');const a=await connectWallet(p),c=clientFor(a as `0x${string}`,p),end=Math.floor(new Date(deadline).getTime()/1000);if(!Number.isFinite(end)||end<=Math.floor(Date.now()/1000))throw Error('Deadline must be in the future');setStatus('SUBMITTING');await writeAndRead(a as `0x${string}`,p,'create_escrow',[planId,recipient,BigInt(amount),BigInt(end)],()=>c.readContract({address:requireContract(),functionName:'get_escrows',args:[0n,50n] as never[]}) as Promise<string[]>,rows=>rows.some(x=>{try{return JSON.parse(x).plan_id===planId&&JSON.parse(x).recipient===recipient}catch{return false}}),(x:LifecycleEvent)=>setStatus(x.phase));setStatus('ESCROW CREATED · CANONICAL READBACK COMPLETE')}catch(e){setStatus(e instanceof Error?e.message:'Escrow creation failed')}};return <div className="shell narrow"><p className="kicker">ESCROW BUILDER / 01</p><h1>Create Escrow</h1><p className="lede">Bind settlement terms to an executable plan authorization.</p><form className="form" onSubmit={submit}><fieldset><legend>01 — BINDING</legend><label>Plan ID<input required value={planId} onChange={e=>setPlanId(e.target.value)}/></label><label>Recipient<input required value={recipient} onChange={e=>setRecipient(e.target.value)} placeholder="0x…"/></label></fieldset><fieldset><legend>02 — TERMS</legend><label>Amount<input required type="number" min="1" value={amount} onChange={e=>setAmount(e.target.value)}/></label><label>Deadline<input required type="datetime-local" value={deadline} onChange={e=>setDeadline(e.target.value)}/></label></fieldset><button className="button primary">CREATE ESCROW →</button><Link className="button" href="/escrow">CANCEL</Link>{status&&<div className="tx-progress">{status}</div>}</form></div>}
