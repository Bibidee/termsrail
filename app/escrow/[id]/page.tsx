export default async function EscrowDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;return <div className="shell"><p className="kicker">ESCROW / {id}</p><h1>Escrow {id}</h1><p className="lede">Funding, policy freeze and settlement state.</p><section className="panel"><h2>CONNECT WALLET TO LOAD CANONICAL ESCROW</h2><p>Execution state, evidence and adjudication will appear from the deployed registry.</p></section></div>}
