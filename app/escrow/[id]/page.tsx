'use client';
import {useParams} from 'next/navigation';
import {useEffect,useState} from 'react';
import {clientFor,connectWallet,getAuthorizedAccount,writeAndRead,requireContract,Eip1193,type LifecycleEvent} from '../../../lib/genlayer';
export default function EscrowDetail(){
 const id=String(useParams<{id:string}>().id??''); const [escrow,setEscrow]=useState<any>(); const [status,setStatus]=useState('LOADING CANONICAL ESCROW…'); const [statement,setStatement]=useState(''); const [urls,setUrls]=useState(''); const [hashes,setHashes]=useState('');
 const load=async()=>{try{const p=(window as Window&{ethereum?:Eip1193}).ethereum;if(!p)throw Error('CONNECT WALLET TO LOAD CANONICAL ESCROW');const a=await getAuthorizedAccount(p);if(!a)throw Error('CONNECT WALLET TO LOAD CANONICAL ESCROW');const c=clientFor(a as `0x${string}`,p);const raw=await c.readContract({address:requireContract(),functionName:'get_escrow',args:[id] as never[]}) as string;if(!raw)throw Error('ESCROW NOT FOUND');setEscrow(JSON.parse(raw));setStatus('CANONICAL ESCROW LOADED')}catch(e){setStatus(e instanceof Error?e.message:'Escrow read failed')}};
 // eslint-disable-next-line react-hooks/exhaustive-deps
 useEffect(()=>{if(id){const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer)}},[id]);
 const mutate=async(method:string,args:unknown[],predicate:(v:string)=>boolean,label:string)=>{try{const p=(window as Window&{ethereum?:Eip1193}).ethereum;if(!p)throw Error('Wallet unavailable');const a=await connectWallet(p),c=clientFor(a as `0x${string}`,p);await writeAndRead(a as `0x${string}`,p,method,args,()=>c.readContract({address:requireContract(),functionName:'get_escrow',args:[id] as never[]}) as Promise<string>,predicate,(x:LifecycleEvent)=>setStatus(x.phase));setStatus(label);await load()}catch(e){setStatus(e instanceof Error?e.message:'Transaction failed')}};
 const submitEvidence=(e:React.FormEvent)=>{e.preventDefault();return mutate('submit_completion',[id,statement,JSON.stringify(urls.split(',').map(x=>x.trim()).filter(Boolean)),JSON.stringify(hashes.split(',').map(x=>x.trim()).filter(Boolean))],v=>JSON.parse(v).status==='UNDER_REVIEW','EVIDENCE SUBMITTED · CANONICAL READBACK COMPLETE')};
 return <div className="shell narrow"><p className="kicker">ESCROW / {id}</p><h1>Escrow {id}</h1><div className="notice">{status}</div>{escrow&&<><section className="panel"><div className="dimension"><span>PLAN</span><strong>{escrow.plan_id}</strong></div><div className="dimension"><span>STATUS</span><strong>{escrow.status}</strong></div><div className="dimension"><span>AMOUNT</span><strong>{escrow.amount}</strong></div><div className="dimension"><span>RECIPIENT</span><strong className="mono">{escrow.recipient}</strong></div></section><div className="actions"><button className="button" onClick={()=>mutate('fund_escrow',[id],v=>v.includes('FUNDED'),'ESCROW FUNDED')}>FUND</button><button className="button" onClick={()=>mutate('lock_escrow',[id],v=>v.includes('LOCKED'),'ESCROW LOCKED')}>LOCK</button><button className="button" onClick={()=>mutate('adjudicate_completion',[id],()=>true,'ADJUDICATION REQUESTED')}>ADJUDICATE</button><button className="button primary" onClick={()=>mutate('release_escrow',[id],v=>v.includes('RELEASED'),'FUNDS RELEASED')}>RELEASE</button><button className="button" onClick={()=>mutate('refund_escrow',[id],v=>v.includes('REFUNDED'),'FUNDS REFUNDED')}>REFUND</button></div></>}<form className="form" onSubmit={submitEvidence}><fieldset><legend>COMPLETION EVIDENCE</legend><label>Statement<textarea required value={statement} onChange={e=>setStatement(e.target.value)}/></label><label>Evidence URLs<input value={urls} onChange={e=>setUrls(e.target.value)} placeholder="https://… (comma separated)"/></label><label>Evidence hashes<input value={hashes} onChange={e=>setHashes(e.target.value)} placeholder="sha256… (comma separated)"/></label></fieldset><button className="button primary">SUBMIT EVIDENCE →</button></form><form className="form" onSubmit={e=>{e.preventDefault();const value=prompt('Describe the dispute');if(value)void mutate('open_dispute',[id,value],v=>v.includes('DISPUTED'),'DISPUTE OPENED')}}><button className="button">OPEN DISPUTE</button></form></div>}
