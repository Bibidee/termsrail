export default function About(){return <div className="shell narrow"><p className="kicker">ABOUT / TRUST BOUNDARY</p><h1>Policy control,<br/><em>not policy theatre.</em></h1><p className="lede">TermsRail is a GenLayer-native execution gate for autonomous agents. It proves consensus over configured public sources; it does not claim those sources are legally controlling or offer legal advice.</p><div className="prose"><h2>Three independent paths</h2><p>Policy snapshots, action authorizations and material-change checks have different inputs, validator work and state transitions. Technical failures remain retryable and never become business verdicts.</p><h2>Fail-closed by construction</h2><p>Execution requires ALLOWED plus fresh policy and authorization, matching source/policy versions, matching specification hash, active policy state and no unresolved material change.</p></div></div>}
